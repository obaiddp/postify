<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit post</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-900 p-10">
        
        <div class="w-full flex justify-end">
            <a class="px-5 py-2 bg-red-500 text-white rounded-md mb-10 hover:bg-red-600 transition-all duration-200" href="/logout">Logout</a>

        </div>

        <h1 class="text-4xl font-bold text-white mb-1">Edit the post</h1>
        
        <form class="flex flex-col gap-6 mb-14" method="post" action="/update/<%= post._id %>">
            <textarea  class="w-1/3 text-white p-3 border-2 rounded-md" placeholder="What's on your mind" name="content" cols="60" rows="10"><%= post.content %></textarea>
            <input class="w-55 h-10 bg-yellow-500 text-black rounded-md" type="submit" value="Update post">
        </form>
    </div>
</body>

</html>